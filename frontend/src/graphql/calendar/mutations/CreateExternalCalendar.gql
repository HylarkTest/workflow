#import "../ExternalCalendarFragment.gql"

mutation CreateExternalCalendar($input: CreateExternalCalendarInput!) {
    createExternalCalendar(input: $input) {
        code
        success
        calendar {
            ...ExternalCalendar
        }
    }
}

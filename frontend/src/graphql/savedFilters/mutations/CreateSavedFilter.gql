#import "../SavedFilterFragment.gql"

mutation CreateSavedFilter($input: SaveFilterInput!) {
    saveFilter(input: $input) {
        code
        success
        savedFilter {
            ...SavedFilter
        }
    }
}

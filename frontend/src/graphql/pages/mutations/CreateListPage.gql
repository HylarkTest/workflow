#import "../PageFragment.gql"

mutation CreateListPage($input: CreateListPageInput!) {
    createListPage(input: $input) {
        code
        success
        page {
            ...Page
            design
        }
    }
}

#import "../FullPageFragment.gql"

mutation UpdateEntitiesPage($input: UpdateEntitiesPageInput!) {
    updateEntitiesPage(input: $input) {
        code
        success
        page {
            ...FullPage
        }
    }
}

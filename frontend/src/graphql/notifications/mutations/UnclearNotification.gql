#import "../NotificationFragment.gql"

mutation UnclearNotification($input: NotificationUnclearInput!) {
    unclearNotification(input: $input) {
        code
        success
        notification {
            ...Notification
        }
    }
}

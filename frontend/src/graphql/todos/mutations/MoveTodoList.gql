#import "../TodoListFragment.gql"

mutation MoveTodoList($input: MoveTodoListInput!) {
    moveList: moveTodoList(input: $input) {
        code
        success
        list: todoList {
            ...TodoList
        }
    }
}

#import "../DriveFragment.gql"

mutation MoveDrive($input: MoveDriveInput!) {
    moveList: moveDrive(input: $input) {
        code
        success
        list: drive {
            ...Drive
        }
    }
}

#import "../DocumentFragment.gql"

mutation MoveDocument($input: MoveDocumentInput!) {
    moveDocument(input: $input) {
        code
        success
        document {
            ...Document
        }
    }
}

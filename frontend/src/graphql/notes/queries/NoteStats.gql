query NoteStats(
    $forMapping: ID,
    $forLists: [ID!],
    $forNode: ID,
) {
    noteStats(
        forMapping: $forMapping,
        forLists: $forLists,
        forNode: $forNode,
    ) {
        favoritesCount
        totalCount
    }
}

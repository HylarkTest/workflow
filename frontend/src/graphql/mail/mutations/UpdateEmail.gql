#import "../EmailFragment.gql"

mutation UpdateEmail($input: UpdateEmailInput!) {
    updateEmail(input: $input) {
        code
        success
        email {
            ...EmailMessage
        }
    }
}

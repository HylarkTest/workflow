#import "../MailboxFragment.gql"

mutation UpdateMailbox($input: UpdateMailboxInput!) {
    updateMailbox(input: $input) {
        code
        success
        mailbox {
            ...Mailbox
            unseenCount
            total
        }
    }
}
